apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-standard
  namespace: dev
spec:
  storageClassName: "standard"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi